# Files excluded from `git archive` output, so the packaged store ZIP
# contains only the extension's runtime files (manifest, scripts, icons).
README.md       export-ignore
store/          export-ignore
docs/           export-ignore
.gitattributes  export-ignore
.gitignore      export-ignore
